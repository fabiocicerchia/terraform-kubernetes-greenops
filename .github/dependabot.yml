version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
    groups:
      # init, analyze and upload-sarif have to move together: CodeQL writes its
      # configuration in one and reads it back in another, and refuses a
      # mismatch. Separate PRs merge at different times and break the scan.
      codeql-action:
        patterns:
          - "github/codeql-action*"
      # Everything else rides in one pull request rather than one each.
      actions:
        patterns: ["*"]
  - package-ecosystem: "terraform"
    directory: "/"
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
